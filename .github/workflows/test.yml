name: Run Tagged Tests

on:
  # Trigger manual
  workflow_dispatch:
    inputs:
      test_suite:
        description: 'Suite de tests a ejecutar'
        required: true
        default: 'Smoke General'
        type: choice
        options:
          - 'Smoke General'
          - 'Onboarding v1'
          - 'Onboarding v2'
          - 'Orders v1'
          - 'Orders v2'
          - 'Pagos'
          - 'Providers'
          - 'Refunds'
          - 'Sintéticos'
          - 'Withdraw Fiat v1'
          - 'Withdraw Fiat v2'
          - 'Withdraw Crypto V1'
          - 'Withdraw Crypto V2'
          - 'Remesas'

  # Ejecución automática en cada push
  push:
    branches:
      - '**' # Se ejecuta en todas las ramas, o especifica ramas específicas como: main, develop, etc.

  # Ejecución automática de Providers los viernes a las 01:30 AM hora Argentina (04:30 UTC)
  schedule:
    - cron: '45 4 * * 5'

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set test suite for scheduled run
        id: set-suite
        run: |
          if [ "${{ github.event_name }}" == "schedule" ]; then
            TEST_SUITE="Providers"
            echo "🕐 Ejecución programada - Suite: Providers"
          elif [ "${{ github.event_name }}" == "push" ]; then
            TEST_SUITE="Onboarding v2"
            echo "🚀 Ejecución por push - Suite: Onboarding v2"
          else
            TEST_SUITE="${{ github.event.inputs.test_suite }}"
            echo "👤 Ejecución manual - Suite: $TEST_SUITE"
          fi
          echo "test_suite=${TEST_SUITE}" >> $GITHUB_OUTPUT

      - name: Map test suite to tags
        id: map-tags
        run: |
          case "${{ steps.set-suite.outputs.test_suite }}" in
            "Smoke General")
              TAGS="@Smoke and @Automated"
              ;;
            "Onboarding v1")
              TAGS="@Onboarding and @Smoke and @Automated"
              ;;
            "Onboarding v2")
              TAGS="@OnboardingV2 and @Smoke and @Automated"
              ;;
            "Orders v1")
              TAGS="@Orders and @Smoke and @V1 and @Automated"
              ;;
            "Orders v2")
              TAGS="@Orders and @Smoke and @V2 and @Automated"
              ;;
            "Pagos")
              TAGS="@QR and @Smoke and @Automated"
              ;;
            "Providers")
              TAGS="@Provider and @Smoke and @Automated"
              ;;
            "Refunds")
              TAGS="@Refunds and @Manual and @Automated"
              ;;
            "Sintéticos")
              TAGS="@Sintetico and @Smoke and @Automated"
              ;;
            "Withdraw Fiat v1")
              TAGS="@Withdraw and @Smoke and @Fiat and @V1 and @Automated"
              ;;
            "Withdraw Fiat v2")
              TAGS="@Withdraw and @Smoke and @Fiat and @V2 and @Automated"
              ;;
            "Withdraw Crypto V1")
              TAGS="@Withdraw and @Smoke and @Crypto and @V1 and @Automated"
              ;;
            "Withdraw Crypto V2")
              TAGS="@Withdraw and @Smoke and @Crypto and @V2 and @Automated"
              ;;
            "Remesas")
              TAGS="@Withdraw and @Smoke and @Remesas and @Automated"
              ;;
            *)
              TAGS="@Smoke and @Automated"
              ;;
          esac

          echo "tags=${TAGS}" >> $GITHUB_OUTPUT
          echo "🏷️ Suite seleccionada: ${{ steps.set-suite.outputs.test_suite }}"
          echo "📋 Tags a ejecutar: ${TAGS}"

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Prepare clean installation
        run: |
          echo "Limpiando solo archivos locales del proyecto..."
          rm -rf node_modules
          rm -f package-lock.json

          echo "Verificando conectividad con CodeArtifact..."
          aws codeartifact describe-repository \
            --domain td \
            --repository backend-shared-libs \
            --region ${{ secrets.AWS_REGION }} \
            --query 'repository.name' \
            --output text

      - name: Configure CodeArtifact authentication
        run: |
          CODEARTIFACT_AUTH_TOKEN=$(aws codeartifact get-authorization-token \
            --domain td \
            --region ${{ secrets.AWS_REGION }} \
            --query authorizationToken \
            --output text)

          CODEARTIFACT_REGISTRY=$(aws codeartifact get-repository-endpoint \
            --domain td \
            --repository backend-shared-libs \
            --format npm \
            --region ${{ secrets.AWS_REGION }} \
            --query repositoryEndpoint \
            --output text)

          echo "registry=${CODEARTIFACT_REGISTRY}" > ~/.npmrc
          echo "${CODEARTIFACT_REGISTRY#https:}:_authToken=${CODEARTIFACT_AUTH_TOKEN}" >> ~/.npmrc
          echo "@md:registry=${CODEARTIFACT_REGISTRY}" >> ~/.npmrc
          echo "fetch-retries=5" >> ~/.npmrc
          echo "fetch-retry-mintimeout=10000" >> ~/.npmrc
          echo "fetch-retry-maxtimeout=60000" >> ~/.npmrc
          echo "fetch-timeout=300000" >> ~/.npmrc

          echo "=== Configuración .npmrc ==="
          cat ~/.npmrc

          unset CODEARTIFACT_AUTH_TOKEN

      - name: Smart dependency installation
        run: |
          echo "=== Método 1: Instalación estándar ==="
          if npm install --fetch-retries=5 --fetch-retry-mintimeout=10000; then
            echo "✅ Instalación exitosa con método estándar"
            exit 0
          fi

          echo "=== Método 2: Reinstalación desde cero ==="
          rm -rf node_modules
          rm -f package-lock.json

          if npm install --fetch-retries=5 --fetch-retry-mintimeout=10000; then
            echo "✅ Instalación exitosa con reinstalación"
            exit 0
          fi

          echo "=== Método 3: Manejo específico del paquete problemático ==="
          if npm info dunder-proto@1.0.1; then
            echo "El paquete existe en el registro, reintentando..."
            npm install dunder-proto@1.0.1 --fetch-retries=5
            npm install --fetch-retries=5
            
            if [ $? -eq 0 ]; then
              echo "✅ Instalación exitosa con manejo específico"
              exit 0
            fi
          fi

          echo "=== Método 4: Último recurso con cache bypass ==="
          rm -rf node_modules
          rm -f package-lock.json

          if npm install --no-cache --fetch-retries=5; then
            echo "✅ Instalación exitosa sin cache"
          else
            echo "❌ Todos los métodos fallaron"
            echo "Información de diagnóstico:"
            npm --version
            node --version
            ls -la
            exit 1
          fi

      - name: Run Cucumber tests - ${{ steps.set-suite.outputs.test_suite }}
        continue-on-error: true
        run: |
          TAGS="${{ steps.map-tags.outputs.tags }}"

          echo "🏷️ Suite: ${{ steps.set-suite.outputs.test_suite }}"
          echo "📋 Ejecutando tests con tags: $TAGS"

          npx cucumber-js --tags "$TAGS" --format json:cucumber-report.json
          node reporter.js

      - name: Generate Test Summary and Prepare Email
        if: always()
        id: test-summary
        run: |
          echo "## 🧪 Resumen de Tests - ${{ steps.set-suite.outputs.test_suite }}" >> $GITHUB_STEP_SUMMARY

          if [ -f cucumber-report.json ]; then
            cat > analyze-results.js << 'EOF'
            const fs = require('fs');
            
            try {
              const report = JSON.parse(fs.readFileSync('cucumber-report.json', 'utf8'));
              
              let totalScenarios = 0;
              let passedScenarios = 0;
              let failedScenarios = 0;
              let totalSteps = 0;
              let passedSteps = 0;
              let failedSteps = 0;
              let skippedSteps = 0;
              let totalDuration = 0;
              
              const failedTests = [];
              
              report.forEach(feature => {
                if (feature.elements) {
                  feature.elements.forEach(scenario => {
                    totalScenarios++;
                    let scenarioFailed = false;
                    
                    if (scenario.steps) {
                      scenario.steps.forEach(step => {
                        totalSteps++;
                        
                        if (step.result) {
                          if (step.result.duration) {
                            totalDuration += step.result.duration;
                          }
                          
                          switch (step.result.status) {
                            case 'passed':
                              passedSteps++;
                              break;
                            case 'failed':
                              failedSteps++;
                              scenarioFailed = true;
                              break;
                            case 'skipped':
                              skippedSteps++;
                              break;
                          }
                        }
                      });
                    }
                    
                    if (scenarioFailed) {
                      failedScenarios++;
                      failedTests.push({
                        feature: feature.name || 'Feature sin nombre',
                        scenario: scenario.name || 'Escenario sin nombre'
                      });
                    } else {
                      passedScenarios++;
                    }
                  });
                }
              });
              
              const duration = Math.round(totalDuration / 1000000);
              const successRate = totalScenarios > 0 ? Math.round((passedScenarios / totalScenarios) * 100) : 0;
              
              console.log('| Métrica | Valor |');
              console.log('|---------|-------|');
              console.log(`| 📊 **Escenarios Totales** | ${totalScenarios} |`);
              console.log(`| ✅ **Escenarios Exitosos** | ${passedScenarios} |`);
              console.log(`| ❌ **Escenarios Fallidos** | ${failedScenarios} |`);
              console.log(`| 📈 **Tasa de Éxito** | ${successRate}% |`);
              console.log(`| 🔧 **Steps Totales** | ${totalSteps} |`);
              console.log(`| ⏱️ **Duración Total** | ${duration}ms |`);
              console.log('');
              
              if (failedTests.length > 0) {
                console.log('### ❌ Tests Fallidos:');
                console.log('');
                failedTests.forEach(test => {
                  console.log(`- **${test.feature}** → ${test.scenario}`);
                });
                console.log('');
              }
              
              if (failedScenarios === 0) {
                console.log('### 🎉 ¡Todos los tests pasaron exitosamente!');
              }
              
              const emailData = {
                testStatus: failedScenarios > 0 ? 'TESTS_FAILED' : 'TESTS_PASSED',
                testSuite: process.env.TEST_SUITE || 'Unknown',
                totalScenarios,
                passedScenarios,
                failedScenarios,
                successRate,
                totalSteps,
                duration,
                failedTests,
                timestamp: new Date().toLocaleString('es-ES', { timeZone: 'America/Argentina/Mendoza' })
              };
              
              fs.writeFileSync('test-results.json', JSON.stringify(emailData, null, 2));
                            
            } catch (error) {
              console.log('❌ Error analizando resultados:', error.message);
              const errorData = {
                testStatus: 'ANALYSIS_ERROR',
                testSuite: process.env.TEST_SUITE || 'Unknown',
                error: error.message,
                timestamp: new Date().toLocaleString('es-ES', { timeZone: 'America/Argentina/Mendoza' })
              };
              require('fs').writeFileSync('test-results.json', JSON.stringify(errorData, null, 2));
            }
          EOF

            TEST_SUITE="${{ steps.set-suite.outputs.test_suite }}" node analyze-results.js >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ No se encontró cucumber-report.json" >> $GITHUB_STEP_SUMMARY
            echo '{"testStatus":"NO_REPORT","testSuite":"${{ steps.set-suite.outputs.test_suite }}","timestamp":"'$(date)'","error":"No se encontró cucumber-report.json"}' > test-results.json
          fi

      - name: Create Slack-friendly summary
        if: always()
        id: slack-summary
        run: |
          if [ -f test-results.json ]; then
            cat > create-slack-summary.js << 'EOF'
            const fs = require('fs');
            const results = JSON.parse(fs.readFileSync('test-results.json', 'utf8'));
            
            let summary = '*📊 Resultados de Tests*\n\n';
            
            if (results.testStatus === 'TESTS_PASSED' || results.testStatus === 'TESTS_FAILED') {
              summary += `✅ *Exitosos:* ${results.passedScenarios}/${results.totalScenarios}\n`;
              summary += `❌ *Fallidos:* ${results.failedScenarios}\n`;
              summary += `📈 *Tasa de éxito:* ${results.successRate}%\n`;
              summary += `⏱️ *Duración:* ${results.duration}ms\n\n`;
              
              if (results.failedTests && results.failedTests.length > 0) {
                summary += '*❌ Tests Fallidos:*\n';
                results.failedTests.slice(0, 5).forEach(test => {
                  // Escapar comillas en el nombre del test
                  const scenarioName = test.scenario.replace(/"/g, '\\"');
                  summary += `• ${scenarioName}\n`;
                });
                if (results.failedTests.length > 5) {
                  summary += `... y ${results.failedTests.length - 5} más\n`;
                }
              } else {
                summary += '*🎉 ¡Todos los tests pasaron exitosamente!*\n';
              }
            } else {
              summary += `⚠️ ${results.error || 'Error al analizar resultados'}\n`;
            }
            
            summary += `\n_Timestamp: ${results.timestamp}_`;
            
            // Escapar caracteres especiales para JSON y eliminar saltos de línea problemáticos
            const output = JSON.stringify(summary);
            console.log(output);
          EOF
            
            SUMMARY=$(node create-slack-summary.js)
            echo "summary=${SUMMARY}" >> $GITHUB_OUTPUT
          else
            SUMMARY=$(echo '"⚠️ No se generaron resultados de tests"' | jq -r .)
            echo "summary=${SUMMARY}" >> $GITHUB_OUTPUT
          fi

      - name: Determine Workflow Status
        if: always()
        id: workflow-status
        run: |
          WORKFLOW_STATUS="SUCCESS"

          if [ ! -f test-results.json ]; then
            WORKFLOW_STATUS="FAILED"
            echo "⚠️ No se generaron resultados de tests"
          else
            TEST_STATUS=$(cat test-results.json | grep -o '"testStatus":"[^"]*' | cut -d'"' -f4)
            if [ "$TEST_STATUS" == "TESTS_FAILED" ] || [ "$TEST_STATUS" == "ANALYSIS_ERROR" ] || [ "$TEST_STATUS" == "NO_REPORT" ]; then
              WORKFLOW_STATUS="FAILED"
            fi
          fi

          echo "workflow_status=${WORKFLOW_STATUS}" >> $GITHUB_OUTPUT
          echo "📊 Estado del Workflow: ${WORKFLOW_STATUS}"

      - name: Send Slack Notification
        if: always()
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          SUITE_NAME: ${{ steps.set-suite.outputs.test_suite }}
          WORKFLOW_STATUS: ${{ steps.workflow-status.outputs.workflow_status }}
          EVENT_TYPE: ${{ github.event_name }}
          ACTOR: ${{ github.actor }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          SUMMARY: ${{ steps.slack-summary.outputs.summary }}
        run: |
          # Determinar emoji y estilo
          if [ "$WORKFLOW_STATUS" == "SUCCESS" ]; then
            EMOJI="✅"
            BUTTON_STYLE="primary"
          else
            EMOJI="❌"
            BUTTON_STYLE="danger"
          fi

          # Determinar tipo de ejecución
          if [ "$EVENT_TYPE" == "schedule" ]; then
            EXEC_TYPE="🕐 Programado"
          elif [ "$EVENT_TYPE" == "push" ]; then
            EXEC_TYPE="🚀 Push"
          else
            EXEC_TYPE="👤 Manual"
          fi

          # Procesar el resumen (remover comillas externas si existen)
          CLEAN_SUMMARY=$(echo "$SUMMARY" | sed 's/^"//;s/"$//')

          # Crear payload JSON
          cat > slack-payload.json << EOF
          {
            "text": "$EMOJI Tests $SUITE_NAME",
            "blocks": [
              {
                "type": "header",
                "text": {
                  "type": "plain_text",
                  "text": "$EMOJI Tests: $SUITE_NAME"
                }
              },
              {
                "type": "section",
                "fields": [
                  {
                    "type": "mrkdwn",
                    "text": "*Suite:*\\n$SUITE_NAME"
                  },
                  {
                    "type": "mrkdwn",
                    "text": "*Estado:*\\n$WORKFLOW_STATUS"
                  },
                  {
                    "type": "mrkdwn",
                    "text": "*Tipo:*\\n$EXEC_TYPE"
                  },
                  {
                    "type": "mrkdwn",
                    "text": "*Ejecutado por:*\\n$ACTOR"
                  }
                ]
              },
              {
                "type": "section",
                "text": {
                  "type": "mrkdwn",
                  "text": $SUMMARY
                }
              },
              {
                "type": "actions",
                "elements": [
                  {
                    "type": "button",
                    "text": {
                      "type": "plain_text",
                      "text": "📊 Ver Detalles Completos"
                    },
                    "url": "$RUN_URL",
                    "style": "$BUTTON_STYLE"
                  }
                ]
              }
            ]
          }
          EOF

          # Enviar a Slack
          curl -X POST -H 'Content-type: application/json' \
            --data @slack-payload.json \
            "$SLACK_WEBHOOK_URL"

      - name: Send Email Notification
        if: always()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 587
          username: ${{ secrets.EMAIL_USERNAME }}
          password: ${{ secrets.EMAIL_PASSWORD }}
          from: ${{ secrets.EMAIL_USERNAME }}
          to: ${{ secrets.NOTIFICATION_EMAIL }}
          subject: "🧪 Tests ${{ steps.set-suite.outputs.test_suite }} - ${{ github.repository }} [${{ steps.workflow-status.outputs.workflow_status || 'UNKNOWN' }}] ${{ github.event_name == 'schedule' && '🕐 [PROGRAMADO]' || github.event_name == 'push' && '🚀 [PUSH]' || '' }}"
          body: |
            Hola!

            Se ejecutaron los tests de la suite "${{ steps.set-suite.outputs.test_suite }}" en el repositorio ${{ github.repository }}

            ${{ github.event_name == 'schedule' && '🕐 Tipo de ejecución: PROGRAMADA (Viernes 01:30 AM Argentina)' || github.event_name == 'push' && '🚀 Tipo de ejecución: AUTOMÁTICA (Push al repositorio)' || '👤 Tipo de ejecución: MANUAL' }}
            📊 Estado del Workflow: ${{ steps.workflow-status.outputs.workflow_status || 'UNKNOWN' }}
            🏷️ Suite ejecutada: ${{ steps.set-suite.outputs.test_suite }}
            📋 Tags utilizados: ${{ steps.map-tags.outputs.tags }}
            ${{ github.event_name == 'push' && format('🌿 Rama: {0}', github.ref_name) || '' }}
            ${{ github.event_name == 'push' && format('👤 Autor: {0}', github.actor) || '' }}

            El workflow de GitHub Actions ${{ steps.workflow-status.outputs.workflow_status == 'SUCCESS' && 'se completó exitosamente' || 'falló durante la ejecución' }}.

            🔗 Ver resultados completos:
            ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}

            📊 Resumen de tests disponible en la pestaña "Summary" del workflow.

            ---
            Generado automáticamente por GitHub Actions

      - name: Upload report artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: cucumber-html-report-${{ steps.set-suite.outputs.test_suite }}-${{ github.run_number }}
          path: |
            cucumber-report.html
            reports/
            cucumber-report.json
            test-results.json
